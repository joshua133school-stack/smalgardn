import Script from "next/script";

export default function Home() {
  return (
    <main className="relative w-full" style={{ height: "400vh" }}>
      <div id="root" className="fixed inset-0 w-screen h-screen" style={{ zIndex: 0 }} />
      <Script src="/index-Cb3RmT86.js" strategy="afterInteractive" />
    </main>
  );
}
